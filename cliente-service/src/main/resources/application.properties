spring.application.name=cliente-service
server.port=8083

spring.datasource.url=jdbc:postgresql://localhost:5432/clientedb
spring.datasource.username=postgres
spring.datasource.password=gabriel18
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.cloud.compatibility-verifier.enabled=false

# Feign Configuration
pedido.service.url=http://localhost:8084  # URL do pedido-service
feign.client.config.pedido-service.connectTimeout=5000
feign.client.config.pedido-service.readTimeout=5000

# Habilita Feign
feign.client.config.default.loggerLevel=basic
